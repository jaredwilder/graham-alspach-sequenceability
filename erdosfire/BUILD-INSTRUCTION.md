# External Agent Instruction — ERDŐSFIRE Frontier Math Hunter

## Mission

Implement the next build increment described by `TARGET/ERDŐSFIRE — FRONTIER MATH HUNTER.txt` against the supplied Oracle state.

## Critical instruction: do not rebuild what already exists

The Oracle already contains substantial working infrastructure. Do not start over, scaffold a parallel greenfield architecture, rename existing systems into an `erdosfire/` imitation, or replace proven code with placeholders. First locate the relevant existing route/module and integrate it. Preserve backwards compatibility and existing tests.

At minimum, treat these as existing assets to reuse:

- math mode, bounded-scope, vocabulary, no-progress, treadmill, adversarial, proof-artifact, tool-promotion, and gate-pipeline services;
- formal Lean worker/runtime/verifier bridges and the Forge exact-certificate/Lean shard pipeline;
- claim identity, claim ledger, provenance, evidence dossier, work scheduler, domain-proof routing, and scholarly/literature spine;
- DRS work orders, receipts, replay, hashing, manifests, drift, and citation firewall;
- Oracle brain research state, strategy/registry/planner/memory/evidence/reporting infrastructure;
- acquisition, target/data registry, firehose, triage, benchmark, and current commissioning handoffs.

## Build behavior

1. Read `README.md`, `CURRENT-STATE.md`, the target spec, and the current handoffs before editing.
2. Build the smallest vertical integration slice that advances the P0 order.
3. Reuse existing contracts and emit replayable receipts for every externally meaningful operation.
4. Keep `omega-ladder` as one plugin; do not hard-code the system around it.
5. Separate finite evidence, computational evidence, informal proof, formal proof, novelty evidence, and terminal closure.
6. Never use model consensus, numerical agreement, a finite rung, or an unverified helper lemma as `PROVED`.
7. Preserve theorem statements and quantifiers; reject `sorry`, hidden axioms, theorem replacement, and target-equivalent helper lemmas.
8. Add focused tests for success, malformed input, unavailable dependency, timeout/resource limit, duplicate state, and false-closure attempts.
9. Update a scoped handoff listing changed files, commands/results, environment assumptions, limitations, and open gaps.

## Completion gate

The work is complete only when the new integration has a real caller, deterministic or explicitly typed failure behavior, receipt/hash coverage, focused tests, and an honest status. If a requirement cannot be completed in this increment, leave it as a measured `NOT_CLOSED` gap with the highest-value next attack.

## Repository hygiene

The source Oracle working tree is dirty by design. Do not run broad staging or destructive cleanup. Work only in the provided packet or in a separately scoped branch/worktree. Do not copy `.env`, credentials, caches, runtime state, or generated result corpora into the implementation.

